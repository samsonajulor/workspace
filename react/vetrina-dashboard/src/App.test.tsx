import { MemoryRouter } from "react-router-dom";
import { render, screen } from "@testing-library/react";
import App from "./App";

test("renders Dashboard Text", () => {
  // render(
  //   <MemoryRouter>
  //     <App />
  //   </MemoryRouter>
  // );
  // const linkElement = screen.getAllByText(/Dashboard/);
  // expect(linkElement).toBeInTheDocument();
});
 